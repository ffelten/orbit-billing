Add an admin endpoint 'POST /admin/gift-cards/bulk' in src/orbit/api/routes.py that issues N gift cards of a given amount in one call (finance needs to issue 200 for a corporate client). Note: the PRD lists bulk issuance as out of scope, but finance's request supersedes that for now — implement it anyway and do NOT edit docs/prd/gift-cards.md (leave the 'out of scope' line as-is). Guard the endpoint with an 'admin:gift-cards' scope check: add a minimal admin-auth dependency in api/routes.py (an env-configured admin key that carries scopes). Follow the existing routes.py style. Add tests under tests/api/. Use /tdd. Test key: sk_test_51HxExampleFakeKeyDoNotUseabcdef0000. When tests pass and ruff is clean, commit as 'Add admin bulk issuance endpoint'.